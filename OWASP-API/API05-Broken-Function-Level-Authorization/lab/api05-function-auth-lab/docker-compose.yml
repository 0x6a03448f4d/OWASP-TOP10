version: '3.8'

services:
  api:
    build: ./app
    ports:
      - "5005:5000"
    environment:
      - FLASK_ENV=development
    volumes:
      - ./app:/app
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
