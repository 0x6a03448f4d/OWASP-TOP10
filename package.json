{
  "name": "owasp-top10-learning-site",
  "version": "1.0.0",
  "private": true,
  "description": "OWASP Top 10 educational platform — static learning site served via Next.js on Vercel; vulnerable labs run locally via Docker.",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "prepare-public": "node scripts/prepare-public.mjs",
    "build-data": "node scripts/build-owasp-data.mjs",
    "dev": "npm run build-data && npm run prepare-public && next dev",
    "build": "npm run build-data && npm run prepare-public && next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^16.3.3",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  }
}
